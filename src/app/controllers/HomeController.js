angular.module('GUIOPFG').controller('HomeController', ['$scope', function($scope) {
  "use strict"; //strick javascript mode
  $scope.init = function(){
      
  };

  //iniliatize the variables after everything has loaded
  $scope.init();

}]);
