angular.module('GUIOPFG').controller('HeaderController', ['$scope', function($scope) {
  "use strict"; //strick javascript mode
  $scope.init = function(){

  };

  //iniliatize the variables after everything has loaded
  $scope.init();

}]);
