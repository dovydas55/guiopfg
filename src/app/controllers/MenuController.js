angular.module('GUIOPFG').controller('MenuController', ['$scope', function($scope) {
  "use strict"; //strick javascript mode
  $scope.init = function(){

  };

  //iniliatize the variables after everything has loaded
  $scope.init();

}]);
